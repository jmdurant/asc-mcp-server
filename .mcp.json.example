{
  "mcpServers": {
    "app-store-connect": {
      "command": "bash",
      "args": ["-c", "set -a && source $HOME/.env && source ./.env 2>/dev/null && exec node $HOME/asc-mcp-server/dist/index.js"]
    }
  }
}
